<form action="{{ route('welcome.home') }}" method="GET" class="space-y-6">
    <div class="pt-4">
        <button type="submit" class="w-full bg-black text-white py-4 font-bold uppercase tracking-widest hover:bg-zinc-800 transition">
            Confirm & Join the Family
        </button>
    </div>
</form>